# Core
pyyaml>=6.0
pydantic>=2.5
python-dotenv>=1.0
tenacity>=8.2
structlog>=24.1

# Inventory
psycopg2-binary>=2.9
sqlalchemy>=2.0
pynetbox>=7.3

# Tier 1: SNMP/LLDP/CDP
pysnmp>=6.2
pysnmp-mibs>=6.2  # optional, for MIB text
netmiko>=4.3        # for SSH show-command scraping
scapy>=2.5          # for WS-Discovery/ONVIF

# Tier 1: ONVIF
zeep>=4.2
onvif-zeep>=0.2     # thin wrapper if available; else use raw zeep

# Tier 1: BioStar 2
requests>=2.31

# Tier 1: Honeywell (config file parsing)
lxml>=5.1

# Tier 2: log mining
pyspark>=3.5
prefixspan>=0.5     # or use SPMF via spmf-py

# Graph
neo4j>=5.18
py2neo>=2021.2.3    # optional alternative driver

# HTTP / UI
fastapi>=0.110
uvicorn>=0.27
httpx>=0.27
jinja2>=3.1

# Tests
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
pyodbc>=5.0.0
pandas>=2.0.0
pydantic>=2.0.0
jinja2>=3.1.0
python-multipart>=0.0.9